/*
 * Інтерфейс classList
 * - add
 * - remove
 * - toggle
 * - replace
 * - contains
 */

//* Робота з інлайновими стилями

// const refs = {
//   navLink: document.querySelector('.js-site-nav-link'),
// };

// console.dir(refs.navLink);

// refs.navLink.style.color = 'crimson';
// refs.navLink.style.fontSize = '22px';
// refs.navLink.style.backgroundColor = 'teal';

// refs.navLink.style.cssText = 'color: crimson; font-size: 22px; background-color: teal;';

//* Робота з класами
// const refs = {
//   backdrop: document.querySelector('.js-backdrop'),
// };

// console.dir(refs.backdrop);

// refs.backdrop.classList.add('is-open');
// refs.backdrop.classList.remove('is-open');

// console.log(refs.backdrop.classList.contains('is-open'));

// if (refs.mobileMenu.classList.contains('is-open')) {
//   refs.mobileMenu.classList.remove('is-open');
// } else {
//   refs.mobileMenu.classList.add('is-open');
// }

// refs.backdrop.classList.toggle('is-open');
// refs.backdrop.classList.toggle('is-open');
